---
interface Props {
  url: string | URL;
}

const { url: href } = Astro.props;
const url = new URL(href);
---

<a href={url.href}>{url.hostname}<span class="path">{url.pathname}</span></a>

<style>
  .path {
    opacity: 0.8;
    font-size: 0.5em;
  }
</style>
